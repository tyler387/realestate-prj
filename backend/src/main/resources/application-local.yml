server:
  error:
    include-message: always

spring:
  flyway:
    validate-on-migrate: ${FLYWAY_VALIDATE_ON_MIGRATE:false}

password-reset:
  token-pepper: ${PASSWORD_RESET_TOKEN_PEPPER:local-password-reset-pepper-change-before-prod}
